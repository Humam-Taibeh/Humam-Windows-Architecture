# ==============================================================
#  Humam Windows Architecture — runtime dependencies
#  Install:  pip install -r requirements.txt
#  Build-time tooling (PyInstaller) lives in requirements-dev.txt
# ==============================================================

# Qt 6 bindings — the entire GUI layer
PySide6>=6.6,<7
